# Android - Officer app (Kotlin)
## Setup
- **JDK:** Use JDK 21.
- **Emulator:** Pixel 7 (API 34) is recommended for this project.

## Commands
Run these from the `android/` folder:

### Build APK
```bash
./gradlew assembleDebug
```

### Install & Run
```bash
./gradlew installDebug
```
