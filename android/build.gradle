buildscript {
    repositories {
        google()
        mavenCentral()
    }
}

plugins {
    // This allows the version to be pulled from gradle.properties
    id 'com.android.application' version "${agpVersion}" apply false
}